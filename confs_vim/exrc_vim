" vim:tabstop=8
" made by: Engells
" date: Jan 31, 2017
" content: lunched configures of vim

let MY_VIM_CONFIG_DIR = "$HOME/.vim/confs"

"exec 'source' expand(MY_VIM_CONFIG_DIR) . '/exrc_vim_plug_add'
exec 'source' expand(MY_VIM_CONFIG_DIR) . '/exrc_shared'
exec 'source' expand(MY_VIM_CONFIG_DIR) . '/exrc_vim_kmap'

" memo for load many configure files
"for CFG_Branch in split(globpath("$HOME/.vim/confs/","*"), '\n')
"	exec 'source' CFG_Branch
"endfor

